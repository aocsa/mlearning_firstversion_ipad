using System;
using UIKit;

namespace MLearning.UnifiedTouch
{
	public class LOSingleSlide : UIView
	{
		/*LOSlideSource source;
		public LOSlideSource Source 
		{
			get 
			{
				return source;
			}
			set 
			{
				source = value;
				initSlide ();
			}
		}

		public LOSingleSlide (int pos) : base ()
		{
			var frame = Constants.ScreenFrame;
			frame.Y = pos * Constants.DeviceHeight;
			Frame = frame;
		}

		void initSlide()
		{
			if (source.Type == 1)
			{
				LeftTextElement slide = new LeftTextElement() { Source = source };
				Add(slide);
			}

			if (source.Type == 2)
			{
				RigthTextElement slide = new RigthTextElement() { Source = source };
				Add(slide);
			}

			if (source.Type == 3)
			{
				ItemizeTextElement slide = new ItemizeTextElement() { Source = source };
				Add(slide);
			}

			if (source.Type == 4)
			{
				TopSlideElement elem = new TopSlideElement() ; 
				Add(elem);

			}

			if (source.Type == 5)
			{
				QuoteTextElement slide = new QuoteTextElement() { Source = source };
				Add(slide);
			}

		}*/
	}
}

